from setuptools import setup, find_packages

setup(
    name="pyrtms",
    version="0.1.0",
    description="A python rewrite of the R rtms package",
    author="Weimin",
    packages=find_packages(),
    python_requires=">=3.10",
)